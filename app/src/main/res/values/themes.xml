<resources>
    <!-- Tema dasar aplikasi -->
    <style name="BaseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorOnBackground">@color/md_theme_onBackground</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>

        <!-- Tambahkan default gaya popup -->
        <item name="popupMenuStyle">@style/Theme.JempolPeduli.PopupOverlay</item>
    </style>

    <!-- Tema untuk aplikasi tanpa ActionBar -->
    <style name="Theme.JempolPeduli.NoActionBar" parent="BaseTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Tema untuk aplikasi dengan ActionBar -->
    <style name="Theme.JempolPeduli.WithAppBar" parent="BaseTheme">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <!-- Tema untuk splash screen -->
    <style name="Theme.App.SplashScreen" parent="BaseTheme">
        <item name="windowSplashScreenBackground">@color/md_theme_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/news_avd_v02</item>
        <item name="postSplashScreenTheme">@style/Theme.JempolPeduli.NoActionBar</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>

    <!-- Overlay untuk AppBar -->
    <style name="Overlay.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Overlay untuk Popup -->
    <style name="Theme.JempolPeduli.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:popupBackground">@color/md_theme_surface</item>
        <item name="android:textColor">@color/md_theme_onSurface</item>
    </style>

    <!-- Fullscreeb theme. -->
    <style name="FullScreenDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
